kind = "web"
previewPath = "/"
title = "DeepClean"
version = "1.0.0"
id = "artifacts/deepclean"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24783

[services.development]
run = "pnpm --filter @workspace/deepclean run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/deepclean", "run", "build" ]
publicDir = "artifacts/deepclean/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24783"
BASE_PATH = "/"
